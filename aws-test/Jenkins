pipeline {
    agent any 
    stages {
        stage('Cloning the project from Github') {
        steps {
                git branch: 'main', 
                url: 'https://github.com/Fwdhus/user.git'
            }
        }
    }
}
